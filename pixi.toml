[workspace]
authors = ["Jared Lumpe <jared@jaredlumpe.com>"]
channels = ["conda-forge"]
name = "snakekit"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]

[pypi-dependencies]
snakekit = { path = ".", editable = true }
